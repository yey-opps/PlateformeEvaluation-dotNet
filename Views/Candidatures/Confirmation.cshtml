@{
    ViewData["Title"] = "Candidature envoyée";
    Layout = null;
}

<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>@ViewData["Title"] - HireDesk</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, #10b981 0%, #059669 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .container {
      background: white;
      border-radius: 24px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.15);
      max-width: 600px;
      width: 100%;
      padding: 64px 48px;
      text-align: center;
    }

    .success-icon {
      width: 96px;
      height: 96px;
      background: linear-gradient(135deg, #10b981, #14b8a6);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 56px;
      margin: 0 auto 32px;
    }

    h1 {
      font-size: 32px;
      color: #111827;
      margin-bottom: 16px;
      font-weight: 700;
    }

    .description {
      color: #6b7280;
      line-height: 1.7;
      margin-bottom: 32px;
      font-size: 16px;
    }

    .info-box {
      background: #f0fdf4;
      border: 2px solid #10b981;
      border-radius: 16px;
      padding: 24px;
      margin-bottom: 32px;
      text-align: left;
    }

    .info-title {
      font-weight: 700;
      color: #065f46;
      margin-bottom: 12px;
      font-size: 16px;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .info-list {
      list-style: none;
      padding: 0;
    }

      .info-list li {
        padding: 8px 0;
        color: #047857;
        font-size: 15px;
        display: flex;
        align-items: flex-start;
        gap: 12px;
      }

        .info-list li::before {
          content: "✓";
          color: #10b981;
          font-weight: 700;
          font-size: 18px;
        }

    .btn-group {
      display: flex;
      gap: 16px;
      justify-content: center;
    }

    .btn {
      display: inline-block;
      padding: 14px 32px;
      border-radius: 12px;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.2s;
      font-size: 15px;
    }

    .btn-primary {
      background: #10b981;
      color: white;
      box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3);
    }

      .btn-primary:hover {
        background: #059669;
        transform: translateY(-2px);
        box-shadow: 0 6px 16px rgba(16, 185, 129, 0.4);
      }

    .btn-secondary {
      background: white;
      color: #10b981;
      border: 2px solid #10b981;
    }

      .btn-secondary:hover {
        background: #f0fdf4;
        transform: translateY(-2px);
      }

    .logo {
      display: inline-flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 32px;
    }

    .logo-icon {
      width: 40px;
      height: 40px;
      background: linear-gradient(135deg, #10b981, #14b8a6);
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 20px;
    }

    .logo-text {
      font-size: 24px;
      font-weight: 700;
      color: #111827;
    }

    .timeline {
      margin: 32px 0;
      text-align: left;
    }

    .timeline-item {
      display: flex;
      gap: 16px;
      margin-bottom: 20px;
      align-items: flex-start;
    }

    .timeline-step {
      min-width: 32px;
      height: 32px;
      background: #d1fae5;
      color: #047857;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      font-size: 14px;
    }

    .timeline-content {
      flex: 1;
      padding-top: 4px;
    }

    .timeline-title {
      font-weight: 600;
      color: #111827;
      margin-bottom: 4px;
    }

    .timeline-desc {
      font-size: 14px;
      color: #6b7280;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">
      <div class="logo-icon">📋</div>
      <span class="logo-text">HireDesk</span>
    </div>

    <div class="success-icon">✅</div>

    <h1>Candidature envoyée avec succès !</h1>

    <p class="description">
      Merci pour votre candidature. Notre équipe RH va examiner votre CV dans les plus brefs délais.
    </p>

    <div class="info-box">
      <div class="info-title">
        📬 Prochaines étapes
      </div>
      <ul class="info-list">
        <li>Votre CV sera examiné par notre équipe RH</li>
        <li>Vous recevrez un email si votre profil est retenu</li>
        <li>Vous pourrez alors passer nos tests d'évaluation en ligne</li>
      </ul>
    </div>

    <div class="timeline">
      <div class="timeline-item">
        <div class="timeline-step">1</div>
        <div class="timeline-content">
          <div class="timeline-title">Examen du CV</div>
          <div class="timeline-desc">2-3 jours ouvrables</div>
        </div>
      </div>
      <div class="timeline-item">
        <div class="timeline-step">2</div>
        <div class="timeline-content">
          <div class="timeline-title">Tests d'évaluation</div>
          <div class="timeline-desc">Si validé, vous recevrez un email</div>
        </div>
      </div>
      <div class="timeline-item">
        <div class="timeline-step">3</div>
        <div class="timeline-content">
          <div class="timeline-title">Entretien</div>
          <div class="timeline-desc">Dernier entretien</div>
        </div>
      </div>
    </div>

    <div class="btn-group">
      <a href="@Url.Action("Index", "Home")" class="btn btn-primary">
        Retour à l'accueil
      </a>
      <a href="@Url.Action("Postuler", "Candidatures")" class="btn btn-secondary">
        Nouvelle candidature
      </a>
    </div>
  </div>
</body>
</html>
