@{
    ViewData["Title"] = "Accueil - Plateforme d'Évaluation";
}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="~/css/home-creative.css">
</head>
<body>

    <!-- HERO SECTION -->
    <section class="home-hero">
        <div class="container">
            <div class="hero-inner">
                <h1>
                    Transformez Votre Processus de Recrutement
                </h1>
                <p class="lead">
                    La plateforme intelligente pour évaluer vos candidats
                </p>
                <p class="hero-description">
                    Une solution moderne et intuitive qui révolutionne la manière dont vous gérez, 
                    évaluez et sélectionnez vos talents. Gagnez du temps, prenez de meilleures 
                    décisions et optimisez votre recrutement avec des outils puissants et élégants.
                </p>
                <div class="hero-cta">
                    @* <a href="@Url.Action("Index", "Dashboard")" class="btn btn-primary">
                        Commencer Maintenant
                    </a> *@
                    <a href="/Identity/Account/Login" class="btn btn-primary">
    Commencer Maintenant
</a>
                    <a href="@Url.Action("Index", "Candidats")" class="btn btn-secondary">
                        Explorer les Candidats
                    </a>
                </div>
            </div>
        </div>
    </section>

    <!-- STATS SECTION -->
    <section class="stats-section" id="stats">
        <div class="container">
            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-icon">👥</div>
                    <h3>2,500+</h3>
                    <p>Candidats Évalués</p>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">✅</div>
                    <h3>5,000+</h3>
                    <p>Évaluations Complètes</p>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">⭐</div>
                    <h3>98%</h3>
                    <p>Taux de Satisfaction</p>
                </div>
                <div class="stat-card">
                    <div class="stat-icon">⚡</div>
                    <h3>-60%</h3>
                    <p>Temps de Recrutement</p>
                </div>
            </div>
        </div>
    </section>

    <!-- FEATURES SECTION -->
    <section class="features-section" id="features">
        <div class="container">
            <div class="section-header">
                <h2 class="section-title">Fonctionnalités Puissantes</h2>
                <p class="section-subtitle">
                    Tous les outils dont vous avez besoin pour un recrutement efficace
                </p>
            </div>
            
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon">🎯</div>
                    <h3>Gestion Centralisée</h3>
                    <p>
                        Gérez tous vos candidats au même endroit avec une interface intuitive. 
                        Filtrez, organisez et accédez rapidement aux profils importants.
                    </p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">📊</div>
                    <h3>Évaluations Avancées</h3>
                    <p>
                        Créez des grilles d'évaluation personnalisées avec scoring automatique. 
                        Suivez les performances en temps réel avec des tableaux de bord détaillés.
                    </p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">📈</div>
                    <h3>Analytics & Rapports</h3>
                    <p>
                        Visualisez vos données avec des graphiques interactifs. 
                        Exportez des rapports complets pour une analyse approfondie.
                    </p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">🎨</div>
                    <h3>Interface Moderne</h3>
                    <p>
                        Design élégant et responsive qui s'adapte à tous les appareils. 
                        Navigation fluide et expérience utilisateur optimale.
                    </p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">🔒</div>
                    <h3>Sécurité Garantie</h3>
                    <p>
                        Vos données sont protégées avec un cryptage de bout en bout. 
                        Conformité RGPD et standards de sécurité les plus élevés.
                    </p>
                </div>

                <div class="feature-card">
                    <div class="feature-icon">⚡</div>
                    <h3>Performances Optimales</h3>
                    <p>
                        Plateforme rapide et réactive capable de gérer des milliers de candidatures. 
                        Recherche instantanée et chargement ultra-rapide.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- RECENT EVALUATIONS SECTION -->
    <section class="recent-section">
        <div class="container">
            <div class="section-header">
                <h2 class="section-title">Évaluations Récentes</h2>
                <p class="section-subtitle">
                    Aperçu des dernières évaluations effectuées
                </p>
            </div>
            
            <div class="recent-grid">
                <div class="eval-card">
                    <h4>Développeur Full Stack</h4>
                    <p class="eval-meta">Il y a 2 heures</p>
                    <div class="eval-stats">
                        <div class="eval-stat-item">
                            <span class="eval-stat-value">8.5</span>
                            <span>Note</span>
                        </div>
                        <div class="eval-stat-item">
                            <span class="eval-stat-value">12</span>
                            <span>Questions</span>
                        </div>
                        <div class="eval-stat-item">
                            <span class="eval-stat-value">45min</span>
                            <span>Durée</span>
                        </div>
                    </div>
                </div>

                <div class="eval-card">
                    <h4>Designer UX/UI</h4>
                    <p class="eval-meta">Il y a 5 heures</p>
                    <div class="eval-stats">
                        <div class="eval-stat-item">
                            <span class="eval-stat-value">9.2</span>
                            <span>Note</span>
                        </div>
                        <div class="eval-stat-item">
                            <span class="eval-stat-value">10</span>
                            <span>Questions</span>
                        </div>
                        <div class="eval-stat-item">
                            <span class="eval-stat-value">35min</span>
                            <span>Durée</span>
                        </div>
                    </div>
                </div>

                <div class="eval-card">
                    <h4>Chef de Projet</h4>
                    <p class="eval-meta">Il y a 1 jour</p>
                    <div class="eval-stats">
                        <div class="eval-stat-item">
                            <span class="eval-stat-value">7.8</span>
                            <span>Note</span>
                        </div>
                        <div class="eval-stat-item">
                            <span class="eval-stat-value">15</span>
                            <span>Questions</span>
                        </div>
                        <div class="eval-stat-item">
                            <span class="eval-stat-value">50min</span>
                            <span>Durée</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA SECTION -->
    <section class="recent-section" style="margin-top: 4rem; margin-bottom: 4rem;">
        <div class="container text-center">
            <div class="section-header">
                <h2 class="section-title">Prêt à Commencer ?</h2>
                <p class="section-subtitle" style="max-width: 700px; margin: 0 auto 2rem;">
                    Rejoignez des centaines d'entreprises qui ont déjà transformé leur processus 
                    de recrutement avec notre plateforme moderne et intuitive.
                </p>
            </div>
            <div class="hero-cta">
                <a href="@Url.Action("Index", "Dashboard")" class="btn btn-primary">
                    Accéder au Dashboard
                </a>
                <a href="@Url.Action("Index", "Evaluations")" class="btn btn-secondary">
                    Créer une Évaluation
                </a>
            </div>
        </div>
    </section>

    <!-- SCRIPTS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <script>
        // Smooth scroll pour les ancres
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Animation au scroll
        const observerOptions = {
            threshold: 0.2,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.style.opacity = '1';
                    entry.target.style.transform = 'translateY(0)';
                }
            });
        }, observerOptions);

        // Observer les cartes de fonctionnalités
        document.querySelectorAll('.feature-card, .stat-card, .eval-card').forEach((card) => {
            observer.observe(card);
        });

        // Effet navbar au scroll
        let lastScroll = 0;
        const navbar = document.querySelector('.navbar');

        window.addEventListener('scroll', () => {
            const currentScroll = window.pageYOffset;
            
            if (currentScroll > 50) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
            
            lastScroll = currentScroll;
        });

        // Animation des statistiques (compteur)
        const animateValue = (element, start, end, duration) => {
            let startTimestamp = null;
            const step = (timestamp) => {
                if (!startTimestamp) startTimestamp = timestamp;
                const progress = Math.min((timestamp - startTimestamp) / duration, 1);
                const value = Math.floor(progress * (end - start) + start);
                element.textContent = value + (end >= 1000 ? '+' : '%');
                if (progress < 1) {
                    window.requestAnimationFrame(step);
                }
            };
            window.requestAnimationFrame(step);
        };

        // Animer les chiffres au scroll
        const statsObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting && !entry.target.classList.contains('animated')) {
                    const value = entry.target.textContent.replace(/[^0-9]/g, '');
                    const numValue = parseInt(value);
                    animateValue(entry.target, 0, numValue, 2000);
                    entry.target.classList.add('animated');
                }
            });
        }, { threshold: 0.5 });

        document.querySelectorAll('.stat-card h3').forEach(stat => {
            statsObserver.observe(stat);
        });
    </script>

</body>
</html>